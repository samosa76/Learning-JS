const student = ["Mari", "Taslim", "Kira", "Nana"];

student.forEach((name) => {
    console.log(`Nama : ${name}`);
    
});

// student.forEach(function (name) {
//    console.log(`Nama ${name}`);
    
// });`